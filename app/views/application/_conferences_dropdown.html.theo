<div class="flex flex-grow items-baseline justify-end">
  <div class="mr-4 text-gray-300 hidden sm:block">Conference</div>
  <div class="w-40">
    <_dropdown title%>
      <_dropdown-link label="ALL" url="/" />
      <% conferences.each do |conference| %>
        <_dropdown-link label%="conference.name" url%="conference_path(conference)" />
      <% end %>
    </_dropdown>
  </div>
<div>
